# AppReveal Agent Rules

## Cross-Platform Parity

- Any new framework functionality that is relevant across supported platforms must be kept in parity across those platforms.
- When adding or changing public behavior, check iOS, Android, macOS, Flutter, and React Native as applicable instead of fixing only one target in isolation.
- If a change is intentionally platform-specific, document the reason and the resulting gap in the same turn.
